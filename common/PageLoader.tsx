import React from "react";
import { PageLoaderIcon } from "../assets/icons/PageLoaderIcon";

const PageLoader = () => {
  return (
    <div className="h-screen flex justify-center items-center">
      <PageLoaderIcon />
    </div>
  );
};

export default PageLoader;
